name: Build TWRP

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Build Environment
        run: |
          sudo apt update
          sudo apt install -y git bc bison build-essential ccache curl flex g++-multilib gcc-multilib gnupg gperf imagemagick lib32ncurses5-dev lib32readline-dev lib32z1-dev libelf-dev liblz4-tool libncurses5 libncurses5-dev libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev python3

      - name: Initialize Repo
        run: |
          mkdir ~/bin
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
          chmod a+x ~/bin/repo
          export PATH=~/bin:$PATH
          mkdir workspace
          cd workspace
          repo init -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_omni -b twrp-12.1
          repo sync -c -j$(nproc) --force-sync --no-clone-bundle --no-tags

      - name: Setup Device Tree
        run: |
          git clone https://github.com/twrpdtgen/android_device_motorola_coful workspace/device/motorola/coful -b coful_g-user-12-S3RWBS32.125-29-2-4-3-3c754f-release-keys

      - name: Build
        run: |
          cd workspace
          source build/envsetup.sh
          lunch omni_coful-userdebug
          mka recoveryimage
